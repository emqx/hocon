cluster = {
  discovery = k8s
  k8s = {
    apiserver = "http://10.110.111.204:8080"
    service_name = emqx
    address_type = ip
    app_name = emqx
    suffix = pod.cluster.local
    namespace = default
  }
}
