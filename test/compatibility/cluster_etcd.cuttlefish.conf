cluster.discovery = etcd
cluster.etcd.server = http://example.com:2379
cluster.etcd.prefix = some_prefix
cluster.etcd.node_ttl = 100m
cluster.etcd.ssl.keyfile = client-key.pem
cluster.etcd.ssl.certfile = client.pem
cluster.etcd.ssl.cacertfile = ca.pem
